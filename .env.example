# Anthropic API key — enables real, streaming AI answers.
# Without it, the app runs in a keyless demo mode that answers from the
# built-in Palo knowledge base with canned retrieval (no external calls).
ANTHROPIC_API_KEY=

# Optional: override the model. Defaults to a current Claude model.
# ANTHROPIC_MODEL=claude-sonnet-5
